import React from "react";
import ReactDOM from "react-dom";

// JSX
const jsxHeading = <h1>Namaste React using JSX 🚀</h1>; // This is JSX.

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(jsxHeading);